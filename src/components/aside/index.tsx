import React from 'react';
import { getRandomInt } from '../../helpers';
import styles from './aside.module.scss';

export const Aside = props => {
  const { index } = props;

  return <aside className="col-md-4">aside</aside>;
};
